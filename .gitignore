# Secrets — never commit
.env.local
.env.*.local
*.pem
*.key

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
.uv/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# Data — large files stay local
core/data/raw/
core/data/parquet/
*.parquet
*.arrow

# Node / web
node_modules/
web/dist/
web/.vite/
.next/

# Convex
convex/.env.local

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Agent-local tooling + memory (not project code)
.claude/
/memory/

# bun init boilerplate — not part of this project
index.ts

# TS incremental build cache
*.tsbuildinfo

# gstack local artifacts (security reports, analytics)
.gstack/
.env.local.bak*
*.bak
core/test_supervisor.py.skip

# local brainstorm/tooling state
.superpowers/
.vercel
.env*

# Plans and specs live on disk, not in the repo
docs/superpowers/
docs/plans/

# Agent-harness files — kept on disk, out of the repo
CLAUDE.md
AGENTS.md
RALPH.md
.mcp.json
.claude/
.agents/

# Convex AI skill packs, installed by `npx convex ai-files install`
skills-lock.json
agent/skills/convex/
agent/skills/convex-*/

# Autonomous-loop tooling from the contest build — kept on disk, out of the repo
scripts/
docs/AUTONOMY.md
convex/_generated/ai/ai-files.state.json
